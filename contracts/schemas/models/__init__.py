"""Canonical Pydantic contracts for VN Terminal Pro.

Owner: Lead integrator (contracts/OWNERSHIP.md). Do not edit outside the
contract-change process in contracts/OWNERSHIP.md Section 24.2.

Every model in this package traces to a section of
vn_terminal_multi_ai_development_plan.md v1.1. Where a field's exact type or
allowed values are not given by the plan, the field is deliberately left as
an open (unconstrained) string or object rather than guessed, and the
ambiguity is recorded in contracts/OPEN_ITEMS.md.
"""
